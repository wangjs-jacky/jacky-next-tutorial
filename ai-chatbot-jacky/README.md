# AI Chatbot - Jacky

学习构建 AI 聊天机器人项目

## 📦 项目初始化状态

项目基础结构已创建完成，包括：
- ✅ Next.js 项目配置
- ✅ TypeScript 配置
- ✅ Tailwind CSS 配置
- ✅ 基础目录结构
- ✅ 基础文件（layout.tsx, page.tsx, globals.css）
- ✅ 工具函数（lib/utils.ts）

## 🚀 安装依赖

由于网络配置问题，请按以下步骤安装依赖：

### 方法 1：使用公共 npm registry（推荐）

在项目根目录创建 `.npmrc` 文件，覆盖全局配置：

```bash
echo "registry=https://registry.npmjs.org/" > .npmrc
```

然后安装依赖：

```bash
pnpm install
```

### 方法 2：使用淘宝镜像

```bash
echo "registry=https://registry.npmmirror.com/" > .npmrc
pnpm install
```

### 方法 3：临时使用公共 registry

```bash
pnpm install --registry https://registry.npmjs.org/
```

## 🏃 运行项目

安装依赖后，启动开发服务器：

```bash
pnpm dev
```

然后在浏览器中访问 http://localhost:3000

## 📁 项目结构

```
ai-chatbot-jacky/
├── app/              # Next.js App Router 目录
│   ├── layout.tsx    # 根布局
│   ├── page.tsx      # 首页
│   └── globals.css   # 全局样式
├── components/       # React 组件
│   └── ui/          # UI 组件
├── lib/             # 工具函数
│   └── utils.ts     # 通用工具函数
├── public/          # 静态资源
├── docs/            # 文档
└── todo/            # 学习计划和进度
```

## ✅ 完成检查清单

完成阶段 0 后，确保：
- [ ] 依赖安装成功（`pnpm install`）
- [ ] 项目可以成功启动（`pnpm dev`）
- [ ] 浏览器可以访问 http://localhost:3000
- [ ] 页面显示正常，没有错误
- [ ] TypeScript 编译没有错误
- [ ] Tailwind CSS 样式正常工作

## 🎯 下一步

完成阶段 0 后，可以开始**阶段 1：基础 UI 搭建**！

参考 `todo/STAGE_0_GUIDE.md` 了解更多详情。

